import React from "react";

const Hero = () => {
  return (
    <section
      id="hero"
      className="h-screen w-full flex items-center justify-center"
    >
      <p className="text-6xl text-white">Hero</p>
    </section>
  );
};

export default Hero;
